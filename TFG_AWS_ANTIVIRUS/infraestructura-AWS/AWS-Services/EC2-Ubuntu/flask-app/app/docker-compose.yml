version: '3.8'

services:
  file-analyzer:
    build: .
    container_name: secure-analyzer
    ports:
      - "5000:5000"
    volumes:
      - ./shared_uploads:/incoming:ro
    tmpfs:
      - /app/uploads:size=100M,exec
    restart: unless-stopped